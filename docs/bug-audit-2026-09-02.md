# Bug audit, 2026-09-02

Hardening pass over the 2.0.3 line, driven by the 2026-09-01 thirteen-worker session logs
(`%APPDATA%\chat-on-steroids\sessions`, twelve most recent sessions) and a read of the
broker, kernel, continuation, goal, tunnel, renderer and extension code. Everything under
"Fixed" is in commit 20046c1 and the merge 94d08a2 (PR #59). Everything under "Open" is a
finding with no code change.

## Fixed

| Area | Symptom | Cause | Fix |
| --- | --- | --- | --- |
| Sub agents | Six workers refused `WORKER_SLEEPING` mid-turn on 2026-09-01 | A sleeping worker's proven call revives it inside a live run, but the run is parked the moment the last worker is thought asleep, and a parked family's worker was refused instead | `kernel.ts::dispatchTracked` calls `reactivateDormantRunForConversation()` before `noteAgentAlive()`; only a parked sleeping, revivable worker's own proven call reactivates, never a prime, a finished/failed worker, a blocked chat, or a family another prime or a handover owns |
| Sub agents | 29 dormant families on disk since Aug 30; every unattributed call waited the 15 s identity window | `dormantRuns` was never pruned | `pruneDormantRuns()` on every park and on restore: newest 16, seven days; evicted worker chats become retired leases |
| Compaction | Chat A refused every tool forever if an automatic handover never landed | Automatic continuations never expired once asked for, and the new `COMPACTION_IN_PROGRESS` refusal keys on them | `askedAt` stamped at the first source-send dispatch; `AUTOMATIC_HANDOVER_TTL_MS` (6 h from `askedAt`) aborts; unasked tickets still have no clock |
| Compaction | A committed record aborted on restart when the session had moved on again (A→B, then B→C) | `restoreContinuations` put it in the "unexpected attachment" branch and cancelled the prime transfer | Committed stays committed |
| Goal | Owed-goal watchdog never nudged a chat again | A draft retired mid-request left `stage='sending'`, so `goalDraftBusy` said busy forever | Acknowledged drafts are never busy |
| Tunnel | "previous tunnel client could not be stopped safely" after a signal kill | `stopTree` checked only `exitCode`; a signal-killed child has `exitCode=null`, `signalCode` set | `signalCode !== null` counts as exited |
| Tools | 40 `STALE_UI_SNAPSHOT: UI snapshot N is no longer active` refusals | `$script:MaxUiSnapshots = 16` is shared by every chat on one helper; thirteen workers evicted each other's newest snapshot | Cap raised to 96 |
| App | No drag-and-drop for folders | Not implemented; a drop navigated the window | `roots:addPath` IPC via `webUtils.getPathForFile`, same `validateNewRoot` as the picker; window swallows other drops |

Regression tests: `test/agents.test.ts` (reactivation, fences, pruning), `test/goal.test.ts`,
`test/tunnel-lifecycle.test.ts`, `test/continuation.test.ts` (deadline, committed restore),
`test/ipc.test.ts` (drop path). `npm run verify` green: 72 files, 2094 tests.

## Open

- **Goal reply obligation does not move on handover.** `moveGoalObjective`/`moveGoalSwitch`
  run on commit; the pending row in `goalReplies` for chat A stays keyed to A and is forgotten
  once A is superseded. The comment in `bridge.ts::inspectOwedGoals` ("the obligation travels
  to the replacement with everything else") is wrong. Effect: one lost nudge; the loop resumes
  after B's first answer. Either move the row or fix the comment.
- **Goal/Loop prompts have no rule for permission questions.** Nothing tells the meta-prompter
  what to do when ChatGPT asks to do something irreversible or outside the stated work (delete,
  force-push, pay, send). Loop example 2 teaches "decide it in one line and push on", which can
  green-light a destructive step. Suggested: one sentence per prompt and trailer, answer only
  what the requirements already decide, refuse or defer anything irreversible the user never
  asked for. Changing a default means moving the current text into the superseded list so
  untouched installs migrate.
- **After a handover the register-mimicry copies the brief.** In chat B the "user" messages
  are the app-typed brief, so "write in the person's own register" copies its formal tone.
- **Terminal continuation retention is measured from `openedAt`.** `sweep()` drops committed
  and aborted records `2 × CONTINUATION_TTL_MS` after opening, so a days-old automatic ticket
  that aborts is forgotten immediately and a replayed ack starts a fresh transaction instead
  of getting "already done". Measure from the settle time.
- **Legacy state files.** 64 `request-correlation-recency-*.json`, `session-cursors.json`,
  `goal-drafts.json` (Aug 30) in the state directory are unreferenced by any code and the
  durable store does not scan the directory. Safe to delete; not deleted.
- **Log observations without a code finding.** `EXEC_RESULTS_UNREAD`, "Invalid session read
  cursor", `write_stdin` exit 1 with empty output, and the model guessing a `/<user>/...` root
  are correct refusals or instruction drift, not app bugs. Phantom `turn_start`/`turn_end`
  flapping (worker-12: 235 turns for 8 request ids) is addressed by the content-script
  `openedUserMessageId` guard that was already in the tree.
- **Static reads only, not exercised here:** Linux/macOS Chrome discovery, POSIX process-group
  kills, tunnel packaging per platform, extension discovery and retry alarm, session listing
  (cached, paged, O(page)). No defects found by reading.

## Second run, 2026-09-02 12:27–13:31 (three workers, compaction window 50 k)

Prime session `2026-09-02-59d89ec6`, four chats, three automatic compactions at 12:38, 12:50 and
13:01 that all landed within ten seconds. The run then degraded from 13:17, and the user's report
("nothing arrived until I brought Chrome forward and clicked through the tabs") reconstructs to
three separate faults plus one user accident.

| Time | What the logs show | Cause | Fix |
| --- | --- | --- | --- |
| 13:17:24 | `computer keypress ctrl+w` from the prime; in the same 100 ms "the prime chat closed"; reopened | The prime was testing its game in a tab of the Chrome window that held its chats; 13:22–13:23 it also walked the tabs with ctrl+tab/ctrl+shift+tab and typed a URL after ctrl+l | `browser-chords.ts` + refusal in `tools-desktop.ts`; desktop instructions say "a window of its own" |
| 13:19:01 → 13:28:00 | "Connection interrupted", turn `stopped`; tool calls keep arriving until 13:26:53; no reload until compaction pickup 3 | Ticket `l5T8LR3z` (opened 13:17:25) owned the recovery clock, so the assistant-error path `break`ed; pickup cadence five minutes | `expediteCompactionPickup()`: the failure brings the next pickup to the next sweep |
| 13:25:56 → 13:28:06 / 13:29:01 | worker-1 and worker-3 reloaded for silence, re-report their thoughts within 12 s, but `turn_end` arrives only when each tab is clicked | Chrome's intensive throttling: hidden ≥ 5 min + chained timers → one wake-up a minute; `every()` was `setInterval`, the activity pull re-armed itself, `sleep()` chained inside retry loops | `later()`/`cancelLater()` MessageChannel hop in `content.js`; `observe()` on `visibilitychange` |
| 13:29:39 | Replacement chat `6a980897`: 0 user messages, 0 assistant messages, 6 tool calls, `AGENTS_BUSY`; ticket armed, never acked | The user's Escape emptied the composer between `destinationDispatch` and the click; `send()` returned false, the resume path returned without a word, `continuationJournalPending` stayed up | Page proves nothing was sent → `/compact destinationLost` → `releaseContinuationDestinationSendNow()` (dispatch and claim back), old command retired, fresh resume queued at once; gate released |

Not bugs: the 12:45:50 "Connection interrupted" on all three workers at once was network-wide and
the app reloaded all three within two seconds; the 12:50 "Message delivery timed out" turns
ended `failed` while the models kept calling tools, which request-id attribution carried without
loss; the `WORKER_IDENTITY_LOST` pair at 13:30–13:31 was one request id retried by ChatGPT from a
page that produced no evidence, after the user had started blocking chats. The reload handout
itself was not late: every repair the app queued was carried out by the extension's next
30-second alarm pass (26 s and 17 s at 13:28:00).

## PR and history

- PR #46 could not merge (four conflicting paths, all the forge's merges of work the branch
  already carried), so no `pull_request` CI ever ran on it. Closed; PR #59 opened from the
  same branch after merging `origin/main` (f8f8c84). The merge dropped one older duplicate
  copy of a content-script test that main's merge reintroduced.
- The two commits carrying the private Gmail address (9e27c0f, 03acfba) are GitHub's own
  merge commits for #19 and #20 and are already on `origin/main`. The privacy gate exempts
  published history and checks everything a push would add. To stop the forge writing that
  address again: GitHub Settings → Emails → "Keep my email addresses private" and "Block
  command line pushes that expose my email". History was not rewritten.
- The Windows x64 build from 20046c1 was installed locally on 2026-09-02 00:47; the version
  string still reads 2.0.2 because `package.json` has not been bumped.
